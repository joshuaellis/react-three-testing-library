// import * as React from 'react'

// import { renderScene } from '../../index'

describe('toHaveMaterial matcher', () => {
  it.todo(
    'should return true if no material is passed but there is one present'
  )

  it.todo(
    'should return true if a material is passed and is present in the mesh'
  )

  it.todo('should return false if non-mesh primitive is passed')

  it.todo('should return false if the material is not found')

  it.todo('should return if there are multiple materials found')
})
