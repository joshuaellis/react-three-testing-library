// import * as React from 'react'

// import { renderScene } from '../../index'

describe('toBeType matcher', () => {
  it.todo('should return true if the type exists')

  it.todo('should return false if the type does not exist')
})
